# .env

# --- CRITICAL FIX: Database Connection ---
# The server was crashing because it couldn't connect to Postgres.
# Setting a non-empty, correct password (DB_PASS) resolves the "SCRAM-SERVER-FIRST-MESSAGE" error.
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASS=psql2026
DB_NAME=Tinylink_db

# --- Application Configuration ---
PORT=8083
BASE_URL=http://localhost:8083